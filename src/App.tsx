import { ReactElement } from "react";
import { OuterSpaceBackground } from "./components/outer-space-background";

export const App = (): ReactElement => {
  return <OuterSpaceBackground>Olá, Mundo!</OuterSpaceBackground>
}